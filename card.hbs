<!-- card.hbs -->
<div class="col-md-4">
    <div class="card">
        <div class="card-icon">
            <i class="{{icon}}"></i>
        </div>
        <div class="card-body">
            <h3>{{title}}</h3>
            <p>{{description}}</p>
        </div>
    </div>
</div>
